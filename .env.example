# Added by Payload
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Maintenance mode - set to 'true' to show maintenance page and skip DB during build
MAINTENANCE_MODE=false

# Database environment - 'local' for local PostgreSQL, 'neon' for cloud
DATABASE_ENV=neon

# Local PostgreSQL connection
LOCAL_DATABASE_URL=postgresql://postgres:password@localhost:5432/chalkandchino

# Neon (cloud) PostgreSQL connection
NEON_DATABASE_URL=your-neon-connection-string-here
