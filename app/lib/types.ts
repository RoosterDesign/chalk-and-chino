export type NavItem = {
    url: string;
    label: string;
    title: string;
};

export type ImageExpanderType = {
    src: string;
    alt: string;
    width: number;
    height: number;
    thumbWidth: number;
    thumbHeight: number;
}
